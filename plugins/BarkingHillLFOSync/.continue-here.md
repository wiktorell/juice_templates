---
plugin: BarkingHillLFOSync
stage: 3
phase: 4.3
status: complete
last_updated: 2026-02-22
complexity_score: 3.4
phased_implementation: true
orchestration_mode: true
next_action: ready_for_install
next_phase: null
contract_checksums:
  creative_brief: sha256:63b51b485667764853f3debde7e5f4818b26058672c9377722d7d56503626fa5
  parameter_spec: sha256:8e1726818dc06d21b35c2b7b8f782518635a6056223ddc4c4ffad228fd833ffa
  architecture: sha256:ad8512815a71bcd014ef42ced89c82f0f25a63a8ad046d23d45b31b90c3e3331
  plan: sha256:26e978a533c2828a274d842b991f3eddbfede45d2a891da070071fce828ca388
---

# Resume Point

## Current State: Stage 3 GUI Complete — All 3 phases done. Ready to install.

Phase 4.1: v7 HTML deployed, BinaryData activated, getResource() implemented.
Phase 4.2: All 7 parameter bindings (getSliderState/getToggleState, valueChangedEvent,
  relative drag, waveform snap, sync display switch, assign animation) — already in v7 HTML.
Phase 4.3: Animated canvas waveform (requestAnimationFrame, 5 waveforms, playhead, glow)
  — already in v7 HTML. All validation passed.

## Completed So Far

**Ideation:** Complete
- Core concept: MIDI-synced LFO modulator via MIDI CC + host automation
- 7 parameters specified and locked in parameter-spec.md

**UI Mockup:** Complete (v7 finalized)
- Window: 700 x 390px
- Aesthetic: Vintage Hardware (warm brass/gold, dark brown, CRT scan lines)
- Layout: full-width oscilloscope display top, controls row bottom

**Stage 0:** Complete
- Plugin type defined: Utility (LFO Modulator), effect plugin with audio passthrough
- Professional examples researched: Xfer LFO Tool, Cableguys MidiShaper, NI Reaktor, Juno-106, JP-8000
- JUCE modules identified: juce_audio_processors, juce_audio_basics, juce_gui_extra
- DSP feasibility verified: all components feasible
- Critical research: MIDI CC VST3 host compatibility (HIGH risk, documented with fallback)
- Assign button feasibility: VST3 direct assignment NOT feasible; v1.0 = UI workflow guide
- Parameter ranges researched and documented
- Complexity score: 3.4
- Strategy: Phased implementation (3 DSP phases, 3 GUI phases)
- Architecture and plan documented

**Stage 3 Phase 4.1:** Complete - WebView integrated, v7 HTML loaded, BinaryData active, getResource() implemented

**Stage 1:** Complete - Build system operational, 8 parameters implemented
- CMakeLists.txt: IS_SYNTH FALSE, NEEDS_MIDI_INPUT TRUE, NEEDS_MIDI_OUTPUT TRUE, NEEDS_WEB_BROWSER TRUE
- APVTS with 7 user parameters + mod_output system parameter
- Stereo input/output bus configuration (effect passthrough)
- State management (getStateInformation/setStateInformation)
- WebView editor stub with correct relay/attachment declarations (pattern #11, #12)
- Pass-through audio (DSP not yet implemented)

**Stage 2 Phase 3.1:** Complete - LFO core + MIDI CC output implemented
- Phase accumulator (lfoPhase [0,1), advanced per processBlock in FREE mode)
- 5 waveform formulas: Sine, Triangle, Square, Saw Up, Saw Down
- Bipolar depth scaler: scaledLFO = lfoOutput * (depth / 100.0f)
- MIDI CC output: one controllerEvent per buffer at offset 0 (channel 1)
- mod_output automation parameter updated via setValueNotifyingHost() each processBlock
- Audio passthrough: buffer unmodified (modulation utility)

**Stage 2 Phase 3.2:** Complete - MIDI clock sync + phase retrigger implemented
- kSubdivisionQN[13] static table (4 BAR to 1/32T, including triplet subdivisions)
- AudioPlayHead::getPosition() (JUCE 8 API) queries bpm, ppqPosition, isPlaying each block
- When sync=true and isPlaying: lfoPhase = fmod(ppq, subdivQN) / subdivQN (transport-locked)
- When sync=true and !isPlaying: phase frozen (phaseAdvanced=true, no free-mode advance)
- When sync=false or playHead unavailable: free-mode phaseInc advance (Phase 3.1 behaviour)
- retrigger=true: scans midiMessages for first note-on, resets lfoPhase=0.0 (overrides sync)
- Rate parameter (0.1-10.0 Hz) mapped to subdivision index via normRate = (hz-0.1)/9.9

**Stage 2 Phase 3.3:** Complete - Edge case hardening + state persistence verified
- sampleRate guard added: processBlock returns early if currentSampleRate <= 0.0
- assign parameter confirmed UI-only; APVTS handles persistence (default=false)
- depth=0 verified: ccVal=63, mod_output=0.5 (correct centre values, no code change needed)
- rate clamping: NormalisableRange + jlimit(0,12) already sufficient
- PluginProcessor.h header updated to reflect Stage 2 complete

## Context to Preserve

- **Architecture file:** `plugins/BarkingHillLFOSync/.ideas/architecture.md`
- **Plan file:** `plugins/BarkingHillLFOSync/.ideas/plan.md`
- **Complexity score:** 3.4 (Complex, phased)
- **DSP components:** LFO Oscillator (custom phase accumulator), MIDI Clock Sync (AudioPlayHead), Phase Retrigger, MIDI CC Output, Bipolar Depth Scaler
- **Critical pattern:** Use `getPosition()` NOT deprecated `getCurrentPosition()` for transport sync
- **Critical limitation:** VST3 MIDI CC blocked in Ableton Live effect slots — mod_output parameter is primary path
- **Implementation strategy:** Phased — DSP phases 3.1/3.2/3.3, GUI phases 4.1/4.2/4.3
- **Bus config:** IS_SYNTH FALSE, NEEDS_MIDI_INPUT TRUE, NEEDS_MIDI_OUTPUT TRUE, stereo passthrough
- **Parameter types:** rate/depth/cc_number = WebSliderRelay; sync/retrigger/assign = WebToggleButtonRelay; waveform = AudioParameterChoice with WebSliderRelay
- **mod_output:** system parameter (index 7 in APVTS), updated via setValueNotifyingHost() each processBlock

## Files Created

- `plugins/BarkingHillLFOSync/.ideas/creative-brief.md` (from ideation)
- `plugins/BarkingHillLFOSync/.ideas/parameter-spec.md` (from mockup finalization, locked)
- `plugins/BarkingHillLFOSync/.ideas/mockups/v7-ui.yaml` (finalized mockup)
- `plugins/BarkingHillLFOSync/.ideas/mockups/v7-ui.html` (production HTML)
- `plugins/BarkingHillLFOSync/.ideas/mockups/v7-PluginEditor.h`
- `plugins/BarkingHillLFOSync/.ideas/mockups/v7-PluginEditor.cpp`
- `plugins/BarkingHillLFOSync/.ideas/mockups/v7-CMakeLists.txt`
- `plugins/BarkingHillLFOSync/.ideas/mockups/v7-integration-checklist.md`
- `plugins/BarkingHillLFOSync/.ideas/architecture.md` (Stage 0)
- `plugins/BarkingHillLFOSync/.ideas/plan.md` (Stage 0)
- `plugins/BarkingHillLFOSync/CMakeLists.txt` (Stage 1)
- `plugins/BarkingHillLFOSync/Source/PluginProcessor.h` (Stage 1)
- `plugins/BarkingHillLFOSync/Source/PluginProcessor.cpp` (Stage 1)
- `plugins/BarkingHillLFOSync/Source/PluginEditor.h` (Stage 1)
- `plugins/BarkingHillLFOSync/Source/PluginEditor.cpp` (Stage 1, updated Stage 3 Phase 4.1)
- `plugins/BarkingHillLFOSync/Source/ui/public/index.html` (Stage 3 Phase 4.1 — v7 HTML)
- `plugins/BarkingHillLFOSync/Source/ui/public/js/juce/index.js` (Stage 3 Phase 4.1 — JUCE bridge)
- `plugins/BarkingHillLFOSync/Source/ui/public/js/juce/check_native_interop.js` (Stage 3 Phase 4.1 — JUCE bridge)

## Next Steps

1. Install plugin to system folders: `/install-plugin BarkingHillLFOSync`
2. Test in DAW (Reaper recommended for MIDI CC output verification)
3. Verify mod_output automation parameter appears in host
4. Verify MIDI clock sync locks to transport at multiple subdivisions
